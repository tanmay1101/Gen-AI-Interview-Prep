incident_id: INC-0002
root_cause_id: RC_DNS_FAILURE
env: prod
region: eastus
service: checkout-api
customer_impact: checkout intermittently fails with immediate 5xx; dependency calls fail quickly
created_at: 2026-03-22T09:20:00Z

## Ticket summary

Checkout started returning `502 Bad Gateway` right after a network change. Payments requests fail immediately and retries never recover.

## What we noticed

- Failures correlate with dependency hostname `payments-sql.internal`.
- Logs show DNS-style errors rather than timeouts.

## Ask

Identify the root cause and return the most relevant evidence.

