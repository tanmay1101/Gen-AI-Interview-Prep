incident_id: INC-0006
root_cause_id: RC_AUTH_INVALID_TOKEN
env: prod
region: eastus
service: api-gateway
customer_impact: many requests fail with 401; users can't authenticate
created_at: 2026-03-26T07:40:00Z

## Ticket summary

After an auth key rotation, clients started failing with `401 Unauthorized`. Token validation logs show signature errors.

## What we need

Identify the root cause and the evidence that supports it.

